spring:
  application:
    name: demoCoffee
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
  datasource:
    username: postgres
    password: Abdu2004
    url: jdbc:postgresql://localhost:5432/postgres
